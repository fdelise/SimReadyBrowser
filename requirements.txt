# Core UI
PyQt5>=5.15
PyQt5-Qt5>=5.15
PyQt5-sip>=12.13

# Numerics (required by OVRTX)
numpy>=1.24

# AWS S3 access
boto3>=1.34
requests>=2.31

# GPU monitoring (optional but recommended)
pynvml>=11.5

# OVRTX is installed separately from NVIDIA's PyPI index:
# pip install ovrtx --extra-index-url https://pypi.nvidia.com

# OVPhysX is optional at runtime; physics controls are disabled if missing.
ovphysx>=0.3.7
